services:
  foxhole-dashboard:
    build: .
    container_name: foxhole-dashboard
    ports:
      - "3000:3000"
    volumes:
      - ./output:/app/output
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
    command: ["node", "src/server-with-tracking.js"]
